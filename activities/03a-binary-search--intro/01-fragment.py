"""

Binary Search is an efficient algorithm to find a target value in a sorted array.

It operates by repeatedly dividing the search space in half, reducing the number of elements to be checked.

It's like searching for a word in a dictionary.

Instead of flipping through every page, you open the dictionary in the middle and see if the word comes before or after that page, then discard the half that doesn’t contain the word.

"""

# this text means "define a machine called binary_search that takes 2 pieces of data, a haystack and a needle"
def binary_search(haystack, needle):

    # inside the machine we can write other code

    print(haystack) # to print out the haystack

    print(needle) # to print out the needle