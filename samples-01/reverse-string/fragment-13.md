# Variant 13: Reverse String Using Zip and Join

This variant uses the `zip` function to pair the string with its reverse and then extracts and joins the characters from the reversed string.

### Characteristics:
- **Time Complexity:** O(n)
- **Space Complexity:** O(n)
- **Style:** Functional, uses zip and join
