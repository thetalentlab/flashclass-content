# Variant 18: Reverse String Using Map and Reversed

This variant uses the `map` function along with `reversed` to iterate over the string in reverse order and join the characters into the final reversed string.

### Characteristics:
- **Time Complexity:** O(n)
- **Space Complexity:** O(n)
- **Style:** Functional, uses map and reversed
